# AtomicRadii

Ion/element radius lookup (`src/AtomicRadii/AtomicRadii.jl`): ion-string
parsing, the ionic/charge-state/bare-element fallback chain, and the
`RadiiSource` lookup contract.

```@autodocs
Modules = [BayeSol.AtomicRadii]
```
