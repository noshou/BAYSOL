# Scattering

The forward model (`src/Scattering/`): spherical-harmonic/Bessel primitives,
the packed partial-wave expansion, per-species scattering terms, the
intensity/Gram-matrix algebra, and the top-level `forward`/`forward_cache`
entry points.

## Scattering

```@autodocs
Modules = [BayeSol.Scattering]
```

## SphFuncs

```@autodocs
Modules = [BayeSol.Scattering.SphFuncs]
```
